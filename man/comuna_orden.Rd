% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comuna_orden.R
\name{comuna_orden}
\alias{comuna_orden}
\title{Orden de comuna}
\usage{
comuna_orden(com, as.factor = TRUE)
}
\arguments{
\item{com}{\code{num} o \code{chr}. CUT comunal, en formato numérico (p. ej. \code{1101}) o
character (p. ej. \code{"01101"}).}

\item{as.factor}{\code{logical}. Por defecto TRUE para crear factor ordenado geográficamente.
FALSE crea variable de clase \code{labelled}.}
}
\value{
Si \code{as.factor = TRUE}, un factor con los niveles de comuna ordenados de
norte a sur (según \link{regiones_y_comunas}). Si \code{as.factor = FALSE}, un vector de
clase \code{labelled} con el CUT comunal y las etiquetas de nombre de comuna. Si algún
valor de \code{com} (no \code{NA}) no corresponde a ninguna comuna conocida, se emite un
\code{warning} y ese valor queda como \code{NA} en el resultado.
}
\description{
Convierte un vector de CUT comunal (numérico o character, con o sin
ceros a la izquierda) al nombre de la comuna, ordenado geográficamente de norte a
sur según el orden de filas de \link{regiones_y_comunas}.
}
\examples{
comuna_orden(c(1101, 5101, 13101))
comuna_orden(c("01101", "05101", "13101"))

}
